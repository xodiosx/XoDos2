# automation-scripts
Generate containers?

automation-scripts


#install-tmoe.sh

Script to install Tmoe.

Run this script on a freshly installed Termux app.

Tested and passed on version 0.119.0-beta.1 on 2025-02-07.

#install-container.sh

Script to install a Debian 12 container, with the parameter being a Tmoe-acceptable container name.

Run this script in Termux where Tmoe is already installed.