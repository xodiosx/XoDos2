<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ================= PERMISSIONS ================= -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:remove="maxSdkVersion"/>
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"/>

    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>

    <!-- ================= APPLICATION ================= -->
    <application
        android:name=".MainApplication"
        android:label="@string/tc_app_name"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/App.Theme"
        android:hardwareAccelerated="true"
        android:extractNativeLibs="true"
        android:usesCleartextTraffic="true"
        android:allowBackup="false"
        android:requestLegacyExternalStorage="true"
        android:supportsRtl="true">

        <!-- ================= FLUTTER MAIN ================= -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/LaunchTheme"
            android:hardwareAccelerated="true"
            android:configChanges="navigation|orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- ================= SIGNAL9 ACTIVITY (was missing) ================= -->
        <activity
            android:name=".Signal9Activity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>

        <!-- ================= X11 MAIN (MUST be in :x11 process) ================= -->
        <activity
            android:name="com.termux.x11.MainActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:supportsPictureInPicture="true"
            android:resizeableActivity="true"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="stateHidden"
            android:configChanges="fontScale|orientation|screenSize|keyboard|keyboardHidden|layoutDirection|locale|navigation|screenLayout|touchscreen|uiMode|smallestScreenSize|density"
            android:theme="@style/AppTheme"
            android:process=":x11"
            tools:node="replace"/>

        <!-- ================= X11 CONTROLLER ACTIVITIES ================= -->
        <activity
            android:name="com.termux.x11.controller.ControlsEditorActivity"
            android:exported="false"
            android:screenOrientation="sensor"
            android:theme="@style/AppTheme"/>
        <activity
            android:name="com.termux.x11.controller.ExternalControllerBindingsActivity"
            android:exported="false"
            android:screenOrientation="sensor"
            android:theme="@style/AppTheme"/>
        <activity
            android:name="com.termux.x11.controller.InputControllerActivity"
            android:exported="false"
            android:screenOrientation="sensor"
            android:theme="@style/AppTheme"/>

        <!-- ================= LORIE PREFERENCES ================= -->
        <activity
            android:name="com.termux.x11.LoriePreferences"
            android:theme="@style/Theme.AppCompat.DayNight"
            android:excludeFromRecents="true"
            android:taskAffinity=".LoriePreferences"
            android:supportsPictureInPicture="false"
            android:resizeableActivity="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.APPLICATION_PREFERENCES"/>
                <action android:name="android.intent.action.MAIN"/>
            </intent-filter>
        </activity>

        <!-- ================= DOCUMENT PROVIDER =================
             ⚠️ This class MUST be present in your APK!
             If you get a ClassNotFoundException for bin.mt.file.content.MTDataFilesProvider,
             you need to add the MT Manager file provider library/module to your project. -->
        <provider
            android:name="bin.mt.file.content.MTDataFilesProvider"
            android:authorities="com.xodos.MTDataFilesProvider"
            android:exported="true"
            android:grantUriPermissions="true"
            android:permission="android.permission.MANAGE_DOCUMENTS">
            <intent-filter>
                <action android:name="android.content.action.DOCUMENTS_PROVIDER"/>
            </intent-filter>
        </provider>

        <!-- ================= RECEIVER ================= -->
        <receiver
            android:name="com.termux.x11.LoriePreferences$Receiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="com.termux.x11.CHANGE_PREFERENCE"/>
            </intent-filter>
        </receiver>

        <!-- ================= HUD SERVICE ================= -->
        <service
            android:name="com.termux.x11.HudService"
            android:enabled="true"
            android:exported="false"/>

        <!-- ================= KEY INTERCEPTOR ================= -->
        <service
            android:name="com.termux.x11.utils.KeyInterceptor"
            android:exported="true"
            android:label="XoDos KeyInterceptor"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService"/>
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config"/>
        </service>

        <!-- ================= PERFORMANCE FLAGS ================= -->
        <meta-data android:name="android.allow_multiple_resumed_activities" android:value="true"/>
        <meta-data android:name="com.samsung.android.multidisplay.keep_process_alive" android:value="true"/>

        <!-- Flutter embedding -->
        <meta-data android:name="flutterEmbedding" android:value="2"/>

    </application>

    <!-- ================= QUERIES ================= -->
    <queries>
        <package android:name="com.xodos"/>
    </queries>

</manifest>