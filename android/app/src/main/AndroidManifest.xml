<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
   >

    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:remove="maxSdkVersion" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:requestLegacyExternalStorage="true"
        android:label="@string/tc_app_name"
        android:name=".MainApplication"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true"
        android:theme="@style/App.Theme"
        android:extractNativeLibs="true"
        android:hardwareAccelerated="true"
        android:supportsRtl="false"
        android:allowBackup="false"
        tools:replace="android:allowBackup,android:label,android:theme,android:supportsRtl">

        <!-- ====== PERFORMANCE METADATA (from old XoDos) ====== -->
        <!-- These signal to Samsung/OnePlus game optimisers to keep GPU at full speed,
             exactly like old XoDos did. They do NOT cause frame caps when combined with
             the opaque theme and task affinity. -->
        <meta-data android:name="com.google.android.gms.games.GAME" android:value="true" />
        <meta-data android:name="com.samsung.android.game.game_home" android:value="true" />
        <meta-data android:name="com.samsung.android.game.game_booster" android:value="true" />
        <meta-data android:name="com.samsung.android.game.gamelist" android:value="true" />

        <!-- Allows multiple activities to be in “resumed” state (essential for smooth task switching) -->
        <meta-data android:name="android.allow_multiple_resumed_activities" android:value="true" />

        <!-- Multi‑window & DeX support (keep process alive) -->
        <meta-data android:name="com.sec.android.support.multiwindow" android:value="true" />
        <meta-data android:name="com.samsung.android.multidisplay.keep_process_alive" android:value="true"
            tools:replace="android:value" />

        <!-- ====== FLUTTER MAIN ACTIVITY (unchanged) ====== -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/LaunchTheme"
            android:configChanges="navigation|orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Signal9Activity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

        <provider
            android:name="bin.mt.file.content.MTDataFilesProvider"
            android:permission="android.permission.MANAGE_DOCUMENTS"
            android:exported="true"
            android:authorities="com.xodos.MTDataFilesProvider"
            android:grantUriPermissions="true">
            <intent-filter>
                <action android:name="android.content.action.DOCUMENTS_PROVIDER" />
            </intent-filter>
        </provider>

        <!-- ====== X11 ACTIVITY – FULL PERFORMANCE OVERRIDE ======
             This completely replaces the module’s activity declaration
             with the exact same settings that old XoDos used. -->
        <activity
            android:name="com.termux.x11.MainActivity"
            android:exported="false"
            android:supportsPictureInPicture="true"
            android:resizeableActivity="true"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:launchMode="singleTask"
            android:theme="@style/AppTheme"
            android:process=":x11"
            android:hardwareAccelerated="true"
            tools:node="replace">
        </activity>

        <!-- Keep the other X11 support activities (unchanged) -->
        <activity
            android:name="com.termux.x11.controller.ControlsEditorActivity"
            android:exported="false"
            android:screenOrientation="sensor"
            android:theme="@style/AppTheme" />
        <activity
            android:name="com.termux.x11.controller.ExternalControllerBindingsActivity"
            android:exported="false"
            android:screenOrientation="sensor"
            android:theme="@style/AppTheme" />
        <activity
            android:name="com.termux.x11.controller.InputControllerActivity"
            android:exported="false"
            android:screenOrientation="sensor"
            android:theme="@style/AppTheme" />

        <!-- Flutter embedding metadata -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
            


    <!-- hud stuff -->

    <service
        android:name="com.termux.x11.HudService"
        android:enabled="true"
        android:exported="false"
        android:process=":x11" />

    </application>
</manifest>